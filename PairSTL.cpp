#include"iostream"
#include"vector"
using namespace std;
main()
{
    pair<int,char> p;//pair STL
    vector<int> v;//vector STL

    p.first=10;//p.first
    p.second='f';//p.second

    // v.push_back(90);
    // v.push_back(80);

    // vector<int>::iterator it;//iterator of vector v

    cout<<"p is "<<p.first<<" "<<p.second<<endl;

    // for(it=v.begin();it!=v.end();it++)
    // {
    //     cout<<*it<<" ";//as "it" is a pointer so dereference it 
    // }
    
}