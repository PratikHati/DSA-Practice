#include"iostream"
#include"climits"
using namespace std;
int minCount(int n)
{
    if(n==1)
        return 1;
    int i=0;
    while(i*i<)
    int a=minCount(n-1);
}
main()
{
    int n;
    cin>>n;
    cout<<minCount(n);
}